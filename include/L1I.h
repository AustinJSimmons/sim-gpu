/*
 * L1 Refers to the level in the memory hierarchy with
 * L1 cache being memory within individual SMs. 'I'
 * just define this module as the dedicated instruction
 * cache as I thought it prudent to differentiate between
 * L1 data cache vs L1 Instruction cache.
 **/
